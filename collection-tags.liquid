      {% if current_tags contains 'herbicides' %}
        <!-- If the 'herbicides' tag is being used to filter the collection, show tag-specific content -->
        <div class="container header-collection-description">Description for Herbicides Tag Here</div>
      {% else %}
        <!-- If not filtering by the 'herbicides' tag, show the general collection description -->
        <div class="container header-collection-description">
          {{ collection.description }}

          {%- if collection.handle == 'lawn-fertilizers' -%}
            <span class="calc-program--trigger desktop-hidden">{{ settings.calc_btn_label }}</span>
          {%- endif -%}
        </div>
      {% endif %}




{% if current_tags %}
  {% assign tag_handled = false %}
  {% for tag in current_tags %}
    {% if tag_handled == false %}
      {% case tag %}
        {% when 'tag1' %}
          <div class="tag-description">Description for Tag 1</div>
          {% assign tag_handled = true %}

        {% when 'tag2' %}
          <div class="tag-description">Description for Tag 2</div>
          {% assign tag_handled = true %}

        {% when 'tag3' %}
          <div class="tag-description">Description for Tag 3</div>
          {% assign tag_handled = true %}

        {# ...add more when clauses for each of your tags up to 'tag20'... #}

        {% when 'tag20' %}
          <div class="tag-description">Description for Tag 20</div>
          {% assign tag_handled = true %}
      {% endcase %}
    {% endif %}
  {% endfor %}

  {% if tag_handled == false %}
    <!-- Fallback content if none of the tags match -->
    <div class="tag-description">Default Collection Description</div>
  {% endif %}
{% else %}
  <!-- If there are no current tags, show the collection's default description -->
  <div class="collection-description">
    {{ collection.description }}
  </div>
{% endif %}
